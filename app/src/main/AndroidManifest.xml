<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ca.choremanager">

    <application>
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="AppTheme"
        <activity android:name=".ChoreView">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ChoreView" />
        <activity
            android:name=".Schedule"
            android:label="@string/title_activity_schedule"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".WeeklyView"
            android:label="@string/title_activity_weekly_view"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Tools"
            android:label="@string/title_activity_tools"
            android:theme="@style/AppTheme.NoActionBar" />
      
        <activity android:name=".Toolss"></activity>
        <activity android:name=".ShoppingChoreView"></activity>
        <activity android:name=".ChoreEditView" /></application>
    </application>
</manifest>